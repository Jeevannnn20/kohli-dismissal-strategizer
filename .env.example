# Optional local override for the project config file.
KOHLI_STRATEGIZER_CONFIG=config.yaml

# Optional cache directory for weather API responses.
WEATHER_CACHE_DIR=data/external/weather_cache

# Optional Streamlit runtime settings.
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=localhost
